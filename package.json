{
  "name": "neurolex-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root package exists only so neon.ts can import @neon/config; the Neon CLI resolves it from the project root. The application's own dependencies live in client/ and server/.",
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1"
  }
}
